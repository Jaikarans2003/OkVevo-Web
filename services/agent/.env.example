OPENROUTER_API_KEY=
PORT=3001
FIREBASE_PROJECT_ID=
FIREBASE_SERVICE_ACCOUNT_JSON=
GROQ_API_KEY=
MANIM_RENDERER_URL=
FIREBASE_STORAGE_BUCKET=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
AWS_REGION=
HYPERFRAMES_BUCKET=
HYPERFRAMES_SFN_ARN=
# Render backend: heygen_cloud (default) | lambda
RENDER_BACKEND=heygen_cloud
HEYGEN_API_KEY=
# One-time secret from POST /v3/webhooks/endpoints (never commit real value)
HEYGEN_WEBHOOK_SECRET=
# Shared HMAC secret for the signed callback token (must match the Next app)
HEYGEN_CALLBACK_SECRET=
# Public HTTPS URL → Next /api/webhooks/heygen (ngrok for local)
HEYGEN_CALLBACK_URL=
# Public HTTPS URL → Next /api/webhooks/fal (same host as HEYGEN_CALLBACK_URL)
FAL_CALLBACK_URL=